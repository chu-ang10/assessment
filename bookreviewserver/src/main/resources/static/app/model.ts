export interface bookDetails{
    title: string
    authors: string[]
    pages:  number
    numofreviewers: number
    genre: string[]
    bookcover: string
}

export interface bookReview{
    title: string
    authors: string[]
    pages: number
    numofreviewers: number
    genre: string[]
    bookcover: string[]
}